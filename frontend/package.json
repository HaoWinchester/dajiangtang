{
  "name": "dajiangtang-recruitment-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vue-tsc --noEmit && vite build",
    "quality": "npm run build && npm run test && npm run quality:e2e:fresh",
    "quality:e2e:fresh": "PLAYWRIGHT_REUSE_SERVER=0 PLAYWRIGHT_PORT=5174 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 playwright test --workers=4",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:fresh": "PLAYWRIGHT_REUSE_SERVER=0 PLAYWRIGHT_PORT=5174 PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174 playwright test --workers=4",
    "test:watch": "vitest"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@testing-library/jest-dom": "^6.6.3",
    "@types/node": "^22.10.0",
    "@vitejs/plugin-vue": "^5.2.4",
    "@vue/test-utils": "^2.4.6",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.2",
    "vite": "^6.3.7",
    "vitest": "^3.2.4",
    "vue-tsc": "^2.2.12"
  }
}
